"use client";
import Script from"next/script";import{useState}from"react";
export function EnquiryForm({propertyReference="",propertyTitle,propertyId,entityType="property",captchaSiteKey,defaultMessage}:{propertyReference?:string;propertyTitle?:string;propertyId?:string;entityType?:"property"|"pg";captchaSiteKey?:string;defaultMessage?:string}){const[state,setState]=useState<{status:"idle"|"loading"|"success"|"error";message?:string}>({status:"idle"});const message=defaultMessage||(propertyTitle?`I am interested in ${propertyTitle}${propertyReference?` (${propertyReference})`:""}. Please contact me.`:"Please contact me about my property requirement.");return <form id="enquiry" className="enquiry-form" onSubmit={async(event)=>{event.preventDefault();setState({status:"loading"});const form=event.currentTarget;try{const response=await fetch("/api/enquiries",{method:"POST",body:new FormData(form),headers:{Accept:"application/json"}});const result=await response.json()as{error?:string;reference?:string};if(!response.ok)throw new Error(result.error||"Unable to send enquiry.");form.reset();const path=window.location.pathname.split("/").filter(Boolean),resolvedType=path[0]==="paying-guest"?"pg":entityType;void fetch("/api/analytics/events",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(propertyId?{eventType:"enquiry_submitted",entityType:resolvedType,entityId:propertyId}:{eventType:"enquiry_submitted",entityType:resolvedType,metadata:{slug:path[1]||path[0]||""}}),keepalive:true});setState({status:"success",message:result.reference?`Enquiry sent. Reference: ${result.reference}`:"Enquiry sent successfully."})}catch(error){setState({status:"error",message:error instanceof Error?error.message:"Unable to send enquiry."})}}}><input type="hidden" name="propertyReference" value={propertyReference}/><label className="honeypot" aria-hidden="true">Website<input name="website" tabIndex={-1} autoComplete="off"/></label><label>Name<input required name="name" autoComplete="name" minLength={2} maxLength={100}/></label><label>Mobile Number<input required name="mobile" type="tel" inputMode="numeric" autoComplete="tel" pattern="[6-9][0-9]{9}" aria-describedby="enquiry-mobile-help"/><small id="enquiry-mobile-help">Enter a 10-digit Indian mobile number starting with 6, 7, 8 or 9.</small></label><label>Email Address <span>(optional)</span><input name="email" type="email" autoComplete="email"/></label><label>Message<textarea required name="message" rows={4} minLength={5} maxLength={2000} defaultValue={message}/></label>{captchaSiteKey&&<><Script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer/><div className="cf-turnstile" data-sitekey={captchaSiteKey}/></>}<button className="button" type="submit" disabled={state.status==="loading"}>{state.status==="loading"?"Sending…":"Send enquiry"}</button><p className={`form-response ${state.status}`} role={state.status==="error"?"alert":undefined} aria-live="polite">{state.message}</p></form>}
