"use client";
import{useEffect}from"react";import{usePathname}from"next/navigation";
export function PublicPageAnalytics(){const pathname=usePathname();useEffect(()=>{const match=pathname.match(/^\/(property|paying-guest)\/([^/]+)$/);if(!match)return;const entityType=match[1]==="paying-guest"?"pg":"property",slug=decodeURIComponent(match[2]);const send=(eventType:string)=>void fetch("/api/analytics/events",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventType,entityType,metadata:{slug}}),keepalive:true});send("listing_view");const click=(event:MouseEvent)=>{const element=(event.target as HTMLElement).closest<HTMLAnchorElement>("a,button");if(!element)return;const href=element instanceof HTMLAnchorElement?element.href:"",text=element.textContent?.toLowerCase()||"";if(href.startsWith("tel:"))send("phone_click");else if(href.includes("wa.me")||text.includes("whatsapp"))send("whatsapp_click");else if(text.includes("share")||text.includes("copy link"))send("share_click")};document.addEventListener("click",click);return()=>document.removeEventListener("click",click)},[pathname]);return null}
