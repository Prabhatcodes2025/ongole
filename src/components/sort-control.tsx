"use client";
import { useRouter, useSearchParams } from "next/navigation";
import { useTransition } from "react";
export function SortControl({value}:{value:string}){const router=useRouter();const params=useSearchParams();const [pending,startTransition]=useTransition();return <label className="sort-control">Sort by<select value={value} disabled={pending} onChange={(event)=>{const query=new URLSearchParams(params.toString());query.set("sort",event.target.value);query.delete("page");startTransition(()=>router.replace(`/properties?${query}`));}}><option value="newest">Newest First</option><option value="oldest">Oldest First</option><option value="price-asc">Price Low to High</option><option value="price-desc">Price High to Low</option><option value="area-asc">Area Low to High</option><option value="area-desc">Area High to Low</option></select><span className="sr-only" aria-live="polite">{pending?"Updating results":""}</span></label>}
