import type {Metadata} from "next";import {redirect} from "next/navigation";import {DashboardShell,DataTable,EmptyState,StatusBadge} from "@/src/components/dashboard/dashboard-shell";import {createSupabaseServerClient} from "@/src/lib/supabase/server";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Listing enquiries",robots:{index:false,follow:false}};
export default async function OwnerEnquiries(){const supabase=await createSupabaseServerClient();const{data:auth}=await supabase.auth.getUser();if(!auth.user)redirect("/login?returnTo=/dashboard/enquiries");const{data:properties}=await supabase.from("properties").select("id,title,reference_no").eq("owner_id",auth.user.id);const ids=(properties||[]).map((item)=>item.id);const{data:items}=ids.length?await supabase.from("enquiries").select("id,reference_no,name,mobile,email,message,status,created_at,property_id").in("property_id",ids).order("created_at",{ascending:false}):{data:[]};const titles=new Map((properties||[]).map((item)=>[item.id,item.title]));return <DashboardShell title="Enquiries" description="Interest received across your property and PG listings." breadcrumbs={[{label:"Dashboard",href:"/dashboard"},{label:"Enquiries"}]}>{items?.length?<DataTable caption="Owner enquiries" headers={["Reference","Listing","Customer","Contact","Message","Status","Received"]}>{items.map((item)=><tr key={item.id}><td>{item.reference_no}</td><td>{titles.get(item.property_id)||"Listing"}</td><td>{item.name}</td><td>{item.mobile}<br/><small>{item.email}</small></td><td className="table-wrap-text">{item.message}</td><td><StatusBadge status={item.status}/></td><td>{new Date(item.created_at).toLocaleDateString("en-IN")}</td></tr>)}</DataTable>:<EmptyState title="No enquiries" description="New enquiries for your published listings will appear here."/>}</DashboardShell>}
