import type {Metadata} from "next";import {redirect} from "next/navigation";import {AdminNavigation} from "@/src/components/admin-navigation";import {createSupabaseServerClient} from "@/src/lib/supabase/server";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Operational reports",robots:{index:false,follow:false}};export default async function ReportsPage(){const supabase=await createSupabaseServerClient();const {data:auth}=await supabase.auth.getUser();if(!auth.user)redirect("/login?returnTo=/admin/reports");const {data:allowed}=await supabase.rpc("has_permission",{required_permission:"analytics.read"});if(!allowed)redirect("/admin");return <main id="main" className="portal-page admin-portal"><div className="shell"><div className="portal-title"><div><p className="eyebrow">Controlled exports</p><h1>Reports</h1><p>Formula-safe CSV and formatted Excel exports, limited to authorised administrators.</p></div></div><AdminNavigation/><section className="report-grid">{["users","properties","enquiries"].map((entity)=><article className="portal-section" key={entity}><h2>{entity[0].toUpperCase()+entity.slice(1)}</h2><p>Export up to 10,000 current records. Every export is audited.</p><div className="portal-actions"><a className="button button-light" href={`/api/admin/reports/${entity}?format=csv`}>CSV</a><a className="button" href={`/api/admin/reports/${entity}?format=xlsx`}>Excel</a></div></article>)}</section></div></main>}
