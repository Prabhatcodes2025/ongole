import type { Metadata } from "next";
import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { createSupabaseServerClient } from "@/src/lib/supabase/server";
export const dynamic="force-dynamic";
export const metadata:Metadata={title:"Review property",robots:{index:false,follow:false}};
export default async function ReviewPropertyPage({params}:{params:Promise<{id:string}>}) { const {id}=await params; const supabase=await createSupabaseServerClient(); const {data:auth}=await supabase.auth.getUser(); if(!auth.user) redirect(`/login?returnTo=/admin/properties/${id}`); const {data:allowed}=await supabase.rpc("has_permission",{required_permission:"properties.read"}); if(!allowed) redirect("/dashboard"); const {data:property}=await supabase.from("properties").select("*,profiles!properties_owner_id_fkey(full_name,reference_no),property_media(id,storage_path,processing_status,is_cover,sort_order)").eq("id",id).single(); if(!property) notFound(); return <main id="main" className="portal-page admin-portal"><div className="shell"><nav className="breadcrumbs"><Link href="/admin">Admin</Link><span>›</span><Link href="/admin/properties">Properties</Link><span>›</span><span>{property.reference_no}</span></nav><div className="portal-title"><div><p className="eyebrow">{property.reference_no}</p><h1>{property.title}</h1><p>{property.locality_text}, {property.city_text} · <span className={`status status-${property.status}`}>{property.status.replaceAll("_"," ")}</span></p></div></div><div className="review-layout"><section className="portal-section"><h2>Submitted information</h2><dl className="review-facts"><div><dt>Owner</dt><dd>{property.profiles?.full_name} ({property.profiles?.reference_no})</dd></div><div><dt>Transaction</dt><dd>{property.transaction_type}</dd></div><div><dt>Price</dt><dd>₹{Number(property.price_inr||0).toLocaleString("en-IN")}</dd></div><div><dt>Area</dt><dd>{property.area_value} {property.area_unit}</dd></div><div className="wide"><dt>Description</dt><dd>{property.description}</dd></div><div className="wide"><dt>Additional fields</dt><dd><pre>{JSON.stringify(property.details,null,2)}</pre></dd></div></dl><h2>Media checks</h2><p>{property.property_media?.length||0} media records. All images must reach <strong>ready</strong> processing status before publication.</p></section><aside className="review-actions"><p className="eyebrow">Decision</p><h2>Review outcome</h2>{property.status==="pending_review"&&<><form action={`/api/admin/properties/${id}/status`} method="post"><input type="hidden" name="action" value="approve"/><button className="button" type="submit">Approve property</button></form><form action={`/api/admin/properties/${id}/status`} method="post"><label>Required changes<textarea required name="reason" rows={4}/></label><button className="button button-light" name="action" value="request_changes">Request changes</button><button className="danger-button" name="action" value="reject">Reject property</button></form></>}{property.status==="approved"&&<form action={`/api/admin/properties/${id}/status`} method="post"><input type="hidden" name="action" value="publish"/><button className="button" type="submit">Publish listing</button></form>}<p className="form-note">Each decision is recorded in status history and the protected audit log.</p></aside></div></div></main>; }
