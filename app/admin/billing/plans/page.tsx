import type{Metadata}from"next";import Link from"next/link";import{redirect}from"next/navigation";import{DashboardShell,DataTable,StatusBadge}from"@/src/components/dashboard/dashboard-shell";import{createSupabaseServerClient}from"@/src/lib/supabase/server";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Manage plans",robots:{index:false,follow:false}};
export default async function PlansAdmin(){const supabase=await createSupabaseServerClient();const{data:auth}=await supabase.auth.getUser();if(!auth.user)redirect("/login?returnTo=/admin/billing/plans");const{data:manage}=await supabase.rpc("has_permission",{required_permission:"plans.manage"});const{data:plans}=await supabase.from("plans").select("*").order("display_order");return <DashboardShell variant="admin" title="Subscription plans" description="Database-configured pricing, limits and feature access." breadcrumbs={[{label:"Admin",href:"/admin"},{label:"Billing",href:"/admin/billing"},{label:"Plans"}]}><section className="dashboard-card"><DataTable caption="Plans" headers={["Plan","Period","Price","Properties","PGs","Images","Analytics","Status",""]}>{plans?.map((plan)=><tr key={plan.id}><td><strong>{plan.name}</strong><br/><small>{plan.slug}</small></td><td>{plan.billing_period}</td><td>₹{Number(plan.price).toLocaleString("en-IN")}</td><td>{plan.listing_limit}</td><td>{plan.pg_listing_limit}</td><td>{plan.image_limit_per_listing}</td><td>{plan.analytics_access?"Yes":"No"}</td><td><StatusBadge status={plan.is_active?"active":"inactive"}/></td><td>{manage&&<Link href={`/admin/billing/plans/${plan.id}`}>Edit</Link>}</td></tr>)}</DataTable></section>{manage&&<section className="dashboard-card dashboard-section"><header><h2>Create plan</h2><p>Prices and limits are stored in the database and immediately affect server-side enforcement.</p></header><form className="submission-form compact-form" action="/api/admin/billing/plans" method="post"><label>Name<input required name="name"/></label><label>Slug<input required name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/></label><label className="wide">Description<textarea name="description"/></label><label>Billing period<select name="billing_period"><option value="monthly">Monthly</option><option value="quarterly">Quarterly</option><option value="yearly">Yearly</option><option value="custom">Custom</option></select></label><label>Interval months<input name="billing_interval_months" type="number" min="0" defaultValue="1"/></label><label>Price<input name="price" type="number" min="0" defaultValue="0"/></label><label>Property limit<input name="listing_limit" type="number" min="0" defaultValue="1"/></label><label>PG limit<input name="pg_listing_limit" type="number" min="0" defaultValue="0"/></label><label>Image limit<input name="image_limit_per_listing" type="number" min="0" max="100" defaultValue="5"/></label><label>Featured allowance<input name="featured_listing_allowance" type="number" min="0" defaultValue="0"/></label><label>Verified allowance<input name="verified_listing_allowance" type="number" min="0" defaultValue="0"/></label><label>Listing duration days<input name="listing_duration_days" type="number" min="1" defaultValue="90"/></label><label>Display order<input name="display_order" type="number" defaultValue="100"/></label><label><input name="is_active" type="checkbox" value="true" defaultChecked/> Active</label><label><input name="is_public" type="checkbox" value="true" defaultChecked/> Public</label><label><input name="enquiry_access" type="checkbox" value="true" defaultChecked/> Enquiries</label><label><input name="analytics_access" type="checkbox" value="true"/> Analytics</label><label><input name="priority_support" type="checkbox" value="true"/> Priority support</label><button className="button">Create plan</button></form></section>}</DashboardShell>}
