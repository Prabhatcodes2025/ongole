import type{Metadata}from"next";import{notFound,redirect}from"next/navigation";import{DashboardShell}from"@/src/components/dashboard/dashboard-shell";import{createSupabaseServerClient}from"@/src/lib/supabase/server";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Edit plan",robots:{index:false,follow:false}};export default async function EditPlan({params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createSupabaseServerClient();const{data:auth}=await supabase.auth.getUser();if(!auth.user)redirect(`/login?returnTo=/admin/billing/plans/${id}`);const{data:allowed}=await supabase.rpc("has_permission",{required_permission:"plans.manage"});if(!allowed)redirect("/admin/billing/plans");const{data:p}=await supabase.from("plans").select("*").eq("id",id).maybeSingle();if(!p)notFound();return <DashboardShell variant="admin" title={`Edit ${p.name}`} description="Changes affect future server-side limit checks and public pricing." breadcrumbs={[{label:"Admin",href:"/admin"},{label:"Billing",href:"/admin/billing"},{label:"Plans",href:"/admin/billing/plans"},{label:p.name}]}><section className="dashboard-card"><form className="submission-form compact-form" action="/api/admin/billing/plans" method="post"><input type="hidden" name="id" value={p.id}/><label>Name<input required name="name" defaultValue={p.name}/></label><label>Slug<input required name="slug" defaultValue={p.slug}/></label><label className="wide">Description<textarea name="description" defaultValue={p.description}/></label><label>Billing period<select name="billing_period" defaultValue={p.billing_period}>{["free","monthly","quarterly","half_yearly","yearly","custom"].map((x)=><option key={x}>{x}</option>)}</select></label><label>Interval months<input name="billing_interval_months" type="number" min="0" defaultValue={p.billing_interval_months}/></label><label>Price<input name="price" type="number" min="0" defaultValue={p.price}/></label><label>Property limit<input name="listing_limit" type="number" min="0" defaultValue={p.listing_limit}/></label><label>PG limit<input name="pg_listing_limit" type="number" min="0" defaultValue={p.pg_listing_limit}/></label><label>Image limit<input name="image_limit_per_listing" type="number" min="0" max="100" defaultValue={p.image_limit_per_listing}/></label><label>Featured allowance<input name="featured_listing_allowance" type="number" min="0" defaultValue={p.featured_listing_allowance}/></label><label>Verified allowance<input name="verified_listing_allowance" type="number" min="0" defaultValue={p.verified_listing_allowance}/></label><label>Listing duration days<input name="listing_duration_days" type="number" min="1" defaultValue={p.listing_duration_days}/></label><label>Display order<input name="display_order" type="number" defaultValue={p.display_order}/></label><label><input name="is_active" type="checkbox" value="true" defaultChecked={p.is_active}/> Active</label><label><input name="is_public" type="checkbox" value="true" defaultChecked={p.is_public}/> Public</label><label><input name="enquiry_access" type="checkbox" value="true" defaultChecked={p.enquiry_access}/> Enquiries</label><label><input name="analytics_access" type="checkbox" value="true" defaultChecked={p.analytics_access}/> Analytics</label><label><input name="priority_support" type="checkbox" value="true" defaultChecked={p.priority_support}/> Priority support</label><button className="button">Save plan</button></form></section></DashboardShell>}
