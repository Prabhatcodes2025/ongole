import type {Metadata} from "next";
import Link from "next/link";
import {Check} from "lucide-react";
import {siteConfig} from "@/src/config/site";
import {getPublicPlans} from "@/src/lib/billing/plans";
export const revalidate=300;
export const metadata:Metadata={title:"Membership plans and pricing",description:"Compare OngoleProperty.com listing, PG, image, promotion and analytics plans.",alternates:{canonical:"/pricing"},openGraph:{title:"Membership plans | OngoleProperty.com",description:"Flexible plans for property and PG owners.",url:"/pricing"}};
export default async function PricingPage({searchParams}:{searchParams:Promise<{reason?:string}>}){const plans=await getPublicPlans();const params=await searchParams;const schema={"@context":"https://schema.org","@type":"Product",name:"OngoleProperty.com Membership",description:"Property and PG listing memberships",offers:plans.filter((plan)=>plan.price>0).map((plan)=>({"@type":"Offer",name:plan.name,price:plan.price,priceCurrency:plan.currency,url:`${siteConfig.url}/pricing#${plan.slug}`,availability:"https://schema.org/InStock"}))};return <main id="main"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><section className="pricing-hero"><div className="shell"><p className="eyebrow">Plans and pricing</p><h1>Choose the reach your listings need</h1><p>All prices and limits come directly from the active plan configuration.</p>{params.reason&&<div className="pricing-alert">Your current plan limit has been reached. Choose a plan with more capacity.</div>}</div></section><section className="shell pricing-grid">{plans.length?plans.map((plan)=><article id={plan.slug} className={`pricing-card ${plan.slug==="professional"?"is-highlighted":""}`} key={plan.id}><header><p>{plan.billing_period.replaceAll("_"," ")}</p><h2>{plan.name}</h2><p>{plan.description}</p><strong>{plan.price?`₹${plan.price.toLocaleString("en-IN")}`:"Free"}<small>{plan.price?"/ billing period":""}</small></strong></header><ul><li><Check/>{plan.listing_limit} property listings</li><li><Check/>{plan.pg_listing_limit} PG listings</li><li><Check/>{plan.image_limit_per_listing} images per listing</li><li><Check/>{plan.listing_duration_days} day listing duration</li><li><Check/>{plan.featured_listing_allowance} featured allowances</li><li><Check/>{plan.verified_listing_allowance} verified allowances</li><li><Check/>{plan.analytics_access?"Owner analytics included":"Standard activity summary"}</li><li><Check/>{plan.priority_support?"Priority support":"Standard support"}</li></ul><Link className="button" href={`/login?returnTo=${encodeURIComponent(`/dashboard/billing?plan=${plan.id}`)}`}>{plan.price?"Choose plan":"Get started"}</Link></article>):<div className="empty-state"><h2>Pricing is temporarily unavailable</h2><p>Plan configuration will appear after the Sprint 5 migration is applied.</p></div>}</section><section className="shell pricing-faq"><h2>Frequently asked questions</h2><details><summary>How are listing limits enforced?</summary><p>Limits are checked by the server and database before a property, PG or image record is created.</p></details><details><summary>When does an online plan activate?</summary><p>Only after the payment provider webhook is verified. A browser redirect cannot activate a subscription.</p></details><details><summary>Can I pay offline?</summary><p>Yes. Submit transaction details and proof from the billing dashboard for administrator review.</p></details><details><summary>What happens when a plan expires?</summary><p>Your records are retained. New creation and publication follow the configured plan and expiration policy.</p></details></section></main>}
