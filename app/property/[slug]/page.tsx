import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { sampleProperties } from "@/src/data/properties";
import { siteConfig } from "@/src/config/site";

export function generateStaticParams() { return sampleProperties.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const property = sampleProperties.find((item) => item.slug === slug); if (!property) return {}; return { title: `${property.title} in ${property.locality}`, description: `${property.type} for ${property.purpose.toLowerCase()} in ${property.locality}, ${property.city}. ${property.area}. ${property.price}.`, alternates: { canonical: `/property/${property.slug}` }, openGraph: { title: property.title, description: `${property.price} · ${property.area} · ${property.locality}`, url: `/property/${property.slug}` } }; }

export default async function PropertyPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params; const property = sampleProperties.find((item) => item.slug === slug); if (!property) notFound();
  const schema = { "@context": "https://schema.org", "@type": "RealEstateListing", name: property.title, url: `${siteConfig.url}/property/${property.slug}`, description: `${property.type} for ${property.purpose.toLowerCase()} in ${property.locality}`, address: { "@type": "PostalAddress", addressLocality: property.locality, addressRegion: "Andhra Pradesh", addressCountry: "IN" } };
  return <main id="main"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema).replace(/</g, "\\u003c") }} /><section className="detail-head shell"><nav className="breadcrumbs" aria-label="Breadcrumb"><Link href="/">Home</Link><span>›</span><Link href="/properties">Properties</Link><span>›</span><span>{property.locality}</span></nav><div className="detail-title"><div><div className="badge-row static"><span className="badge">Verified</span>{property.featured && <span className="badge badge-warm">Featured</span>}</div><p className="property-meta">{property.id} · {property.category} · {property.type}</p><h1>{property.title}</h1><p>{property.locality}, {property.city}, Andhra Pradesh</p></div><div className="detail-price"><strong>{property.price}</strong><span>{property.area}</span></div></div></section><section className="shell detail-grid"><div><div className={`detail-gallery property-visual ${property.color}`}><span className="visual-sun" /><span className="visual-building" /><span className="visual-ground" /><p>Property imagery is published after review and optimisation</p></div><div className="detail-section"><h2>Property overview</h2><div className="fact-grid"><div><span>Purpose</span><strong>{property.purpose}</strong></div><div><span>Property type</span><strong>{property.type}</strong></div><div><span>Area</span><strong>{property.area}</strong></div><div><span>Status</span><strong>Available</strong></div></div></div><div className="detail-section"><h2>Highlights</h2><ul className="highlight-list">{property.highlights.map((item) => <li key={item}>✓ {item}</li>)}</ul><p>This sample listing demonstrates the production display architecture. Public records are supplied only by approved database entries; empty fields are not rendered.</p></div></div><aside className="enquiry-card"><p className="eyebrow">Interested in this property?</p><h2>Request details or a visit</h2><p>Your enquiry goes to OngoleProperty.com. Private owner numbers are never exposed without an authorised visibility rule.</p><form action="/api/enquiries" method="post"><input type="hidden" name="propertyReference" value={property.id} /><label>Name<input required name="name" autoComplete="name" /></label><label>Mobile<input required name="mobile" type="tel" inputMode="tel" autoComplete="tel" /></label><label>Email<input name="email" type="email" autoComplete="email" /></label><label>Message<textarea name="message" rows={4} defaultValue={`I am interested in ${property.id}. Please contact me.`} /></label><button className="button" type="submit">Send enquiry</button></form><div className="contact-options"><a href={siteConfig.phoneHref}>Call OngoleProperty</a><a href={siteConfig.whatsappHref}>WhatsApp our team</a></div></aside></section></main>;
}
